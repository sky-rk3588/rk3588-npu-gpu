[Unit]
Description=NPU brzi start (rocket devfreq + 850 mV + IRQ + governori)
After=systemd-modules-load.service basic.target

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/npu-fast-boot.sh
RemainAfterExit=yes
TimeoutStartSec=90

[Install]
WantedBy=multi-user.target
